Extract professional profile data from the resume below. Return ONLY a valid JSON object — no explanation, no markdown, no code fences.

The JSON must have exactly these top-level keys:
- "skills": array of {name, category ("technical"|"soft"|"domain"|"language"), proficiency ("expert"|"advanced"|"intermediate"|"beginner"), years_experience (number), keywords (string)}
- "experiences": array of {company, title, start_date (YYYY-MM), end_date (YYYY-MM or null if current), location, description, is_remote (bool), achievements: [{description, metrics, skills_used}]}
- "education": array of {institution, degree, field, start_date (YYYY-MM), end_date (YYYY-MM), gpa}
- "certifications": array of {name, issuer, date_obtained (YYYY-MM), expiry_date (YYYY-MM)}
- "projects": array of {name, description, url, skills_used, highlights}

Rules:
- Extract every skill mentioned. Infer proficiency from context.
- For dates, use YYYY-MM. If only year given, use YYYY-01 for start and YYYY-12 for end.
- If end_date is missing on the most recent role, use null.
- Only include certifications explicitly listed, not general skills.
- Use empty arrays [] for sections not present in the resume.

RESUME:
{resume_text}

JSON:
