"""Phase 3: resume tailoring and DOCX/PDF generation."""


async def generate_resume(job_id: int) -> tuple[str, str]:
    """Generate resume for job. Returns (docx_path, pdf_path). Phase 3 TODO."""
    raise NotImplementedError("Phase 3")
