"""Phase 2: compatibility scoring between a job and the user's profile."""


async def score_job(job_id: int) -> float:
    """Score a single job. Returns compatibility 0.0–1.0. Phase 2 TODO."""
    raise NotImplementedError("Phase 2")
